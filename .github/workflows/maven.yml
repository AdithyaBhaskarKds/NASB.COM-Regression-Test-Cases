# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
   # branches: 
   # - main
   # - 'NASB.COM/**'
  #pull_request:
   # branches: 
   # - main
   # - 'NASB.COM/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: echo "Done"

    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    #- name: Update dependency graph
    #  uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
    - name: Submit results to Xray
      uses: mikepenz/xray-action@v0.9.4
      with:
        username: ${{ secrets.JIRA_USERNAMEVA }}
        password: ${{ secrets.TOKENVA }}
        testFormat: "junit"
        testPaths: "**/surefire-reports/*.xml"
        projectKey: "QAHP-3970"
 
      #env:
       # JIRA_SERVER_URL: ${{ secrets.jira_server_url }}
       # JIRA_USERNAME: ${{ secrets.jira_username }}
       # JIRA_PASSWORD: ${{ secrets.jira_password }}
      #run: 'curl -H "Content-Type: multipart/form-data" -v https://mysite.atlassian.net --user abhaskar@nasb.com:$TOKEN  -F "file=@target/surefire-reports/main.result.xml" "$JIRA_SERVER_URL/rest/raven/1.0/import/execution/junit?projectKey=QAHP-3970"'
      #run: curl -v https://mysite.atlassian.net --user abhaskar@nasb.com:$TOKEN
